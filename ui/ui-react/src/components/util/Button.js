import { Button as BulmaButton } from "bloomer/lib/elements/Button";
import styled from "styled-components";
const Button = styled(BulmaButton)`
  white-space: normal;height:auto
`;

export default Button;

